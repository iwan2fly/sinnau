spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/sinnau_user_db
    username: iwan2fliy
    password: alpha13579
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect